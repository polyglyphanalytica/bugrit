# Skip Puppeteer's Chrome download during npm install
# In Docker deployments, native Chromium is installed via the Dockerfile
# This prevents redundant downloads and ensures consistent browser versions
# For local development, install Chrome/Chromium manually or set PUPPETEER_SKIP_DOWNLOAD=false
PUPPETEER_SKIP_DOWNLOAD=true
